
============================================================================
vendor 目录说明
============================================================================
主要包含 Laravel 应用程序的外部依赖库，包括 Laravel 框架源代码部分。
该目录中文件的组织结构是根据 依赖关系决定的，每一个文件夹都是一个功能模块，
可以单独通过 composer 下载该组件进行使用，对于像 Laravel 框架这样的大组件
还会包含很多的小组件，相当于整个 vendor 目录就是由一个个相互依赖的功能组件
模块组织起来的，它们可以独立工作，也可以补组织起来协调工作。
============================================================================

bin
执行目录
----------------------------------------

composer
加载目录。
主要包含 composer 按照 PSR 规范生成的自动加载类。
应用程序类的自动加载都是由这部分实现的。
----------------------------------------

dnoegel
----------------------------------------

doctrine
----------------------------------------

dragonmantank
----------------------------------------

egulias
----------------------------------------

fideloper
----------------------------------------

filp
----------------------------------------

fzaninotto
----------------------------------------

hamcrest
----------------------------------------

jackub-onderka
----------------------------------------

laravel
核心框架源代码。
代码部分都包含在 vendor\laravel\framework\src\Illuminate文件夹下，在该文件夹下又包含很多
文件夹，每一个文件夹又是一个组件，如用于管理 session 的 session 组件、用于实现路由功能的
routing 组件，这些组件都是可以独立工作的。
----------------------------------------

league
Frank de Jonge开发的PHP包 Flysystem 提供了强大的文件系统抽象层。Laravel集成 Flysystem以便使用不同驱动简化对
文件系统的操作，这些驱动包括本地文件系统、Amazon S3以及 Rackspace 云存储。此外，这些存储选项之间切换非常简单，
因为对不同系统而言，API是一致的。
----------------------------------------

mockery
Mockery 是简单而灵活的 PHP mock 对象框架，常用在 PHPUnit，PHPSpec 或者其他测试框架的单元测试中。
它的核心目标是提供一个双向测试框架，提供一个succint API，能清晰的定义所有可能的对象操作和交互，使用人类可读的
Domain Specific Language(DSL)。
----------------------------------------

monolog
包含日志记录模块文件。
一个为5.3以上版本php开发的日志库。但是需要注意的是现在主干版本只支持php7以上版本。
----------------------------------------

myclabs
DeepCopy 深拷贝
----------------------------------------

nesbot
Carbon	日期时间
----------------------------------------

nikic
php-parser	
是一个用 PHP 编写的 PHP 解析器，该项目的目的是简化静态代码分析和操作。
----------------------------------------

nunomaduro
collision 
是一个用于控制台/命令行PHP应用错误处理框架
----------------------------------------

opis
closure
----------------------------------------

paragonie
random_compat
----------------------------------------

phar-io
----------------------------------------

phpdocumentor
phpDocumentor是用php语言写的一个非常强大的文档自动生成工具，利用它可以帮助我们编写规范的注释，生成易于理解，结构清晰的文档，对我们的代码升级，维护，移交等都有非常大的帮助。
----------------------------------------

phpoption
----------------------------------------

phpspec
prophecy 是功能强劲且灵活的模拟框架。尽管它最初是用来满足 phpspec2 的需求，但是它足够灵活，并且可以在任何测试框架中使用。
----------------------------------------

phpunit
包含程序单元测试模块文件。
它是一款轻量级的 PHP 测试框架。
1、可以通过命令操控测试脚本 
2、可以测试性能
3、可以测试代码覆盖率
4、可以自动化的更新测试用例的参数数据
5、各种格式的日志
----------------------------------------

psr
----------------------------------------

psy
psysh 
PsySH 是一个PHP的运行时开发平台，交互式调试器和 Read-Eval-Print Loop (REPL)。
简单点，就像你用 firebug 的 console 调试你的 JavaScript 代码一样。
----------------------------------------

ramsey
uuid 唯一识别码
----------------------------------------

scrivo
highlight.php 高亮
----------------------------------------

sebastian
----------------------------------------

swiftmailer
swiftmailer PHP Mailer
----------------------------------------

symfony
Laravel 框架的底层（如请求类、响应类、文件管理类等）使用了 symfony 框架的部分，所以该
目录包含这部分内容。
----------------------------------------

theseer
tokenizer  用于将标记化的PHP源代码转换为XML
----------------------------------------

tijsverkoyen
css-to-inline-styles 是一个类，它能够将 HTML 页面/文件转换为具有内联样式的 HTML 页面/文件。这在发送电子邮件时非常有用。
----------------------------------------

vlucas
phpdotenv 读取 env 文件
----------------------------------------

webmozart
assert 
----------------------------------------